# File: .readthedocs.yaml

version: 2

sphinx:
  configuration: doc/conf.py

python:
  version: 3.8
  install:
    - method: pip
      path: .
      extra_requirements:
        - dev

# Build PDF & ePub
formats:
  - epub
  - pdf
