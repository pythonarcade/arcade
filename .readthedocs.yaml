# File: .readthedocs.yaml

version: 2

sphinx:
  configuration: doc/conf.py

build:
  os: ubuntu-24.04
  tools:
    python: "3.13"

python:
  install:
    - method: uv
      command: sync
      groups:
        - dev
        - docs

# Build PDF & ePub
# formats:
#  - epub
#  - pdf
